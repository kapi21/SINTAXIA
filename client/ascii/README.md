# ASCII — respaldo / misma copia que client/

Sincronizado con los `.bas` de `client/` (no editar solo aqui: edita en `client/` y vuelve a copiar).

Para la SD puedes usar esta carpeta o `client/` indistintamente, mas:

- `TITLE.SCR` (MODE 1)
- `TITLE2.SCR` (MODE 2, opcional)
